# Runtime requirements for the deployed Agent Engine instance.
# Deliberately excludes test-only deps (pytest*). Chromium itself is installed
# by installation_scripts/install.sh at build time, not via pip.
google-adk>=1.0.0
google-cloud-aiplatform[agent_engines,adk]>=1.95.0
google-generativeai>=0.8.0
playwright>=1.44.0
mcp>=1.0.0
axe-playwright-python>=0.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
rich>=13.0.0
